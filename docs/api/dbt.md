# dbt Renderer

Compiles dbt models via subprocess (`dbt compile`), then parses the compiled SQL with SqlParser.

::: sqlprism.languages.dbt.DbtRenderer
    options:
      members_order: source
      show_root_heading: true
      show_root_full_path: false
