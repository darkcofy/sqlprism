# SQLMesh Renderer

Renders SQLMesh models into clean SQL via subprocess, then parses with SqlParser.

::: sqlprism.languages.sqlmesh.SqlMeshRenderer
    options:
      members_order: source
      show_root_heading: true
      show_root_full_path: false
